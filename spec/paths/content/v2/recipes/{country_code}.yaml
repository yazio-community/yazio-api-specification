get:
  tags:
  - Content
  operationId: listFeaturedRecipes
  summary: Editorial recipe collections for a country
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../../../components/schemas/RecipeIndexEntry.yaml
  parameters:
  - name: country_code
    in: path
    required: true
    schema:
      type: string
