get:
  tags:
  - Buddies
  operationId: getBuddy
  summary: A buddy's profile and recent activity
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/Buddy.yaml
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
