get:
  tags:
  - User
  operationId: getUser
  summary: The authenticated user's profile
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/UserProfile.yaml
