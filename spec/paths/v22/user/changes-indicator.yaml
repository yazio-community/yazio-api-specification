get:
  tags:
  - User
  operationId: getChangesIndicator
  summary: Timestamps telling a client what to re-fetch
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/ChangesIndicator.yaml
