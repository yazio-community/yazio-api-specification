get:
  tags:
  - Goals
  operationId: getGoals
  summary: The user's daily goals
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/DailyGoals.yaml
  parameters:
  - name: date
    in: query
    required: false
    schema:
      type: string
