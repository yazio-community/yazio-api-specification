get:
  tags:
  - User
  operationId: getKeyValueStore
  summary: Arbitrary client-stored settings
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/KeyValueStore.yaml
