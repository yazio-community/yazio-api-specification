post:
  tags:
  - Products
  operationId: createUserProduct
  summary: Create a user product
  responses:
    '204':
      description: Created
      content: {}
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../../components/schemas/UserProductDraft.yaml
get:
  tags:
  - Products
  operationId: listUserProducts
  summary: Products the user created
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              type: string
