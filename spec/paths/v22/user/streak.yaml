get:
  tags:
  - Streaks
  operationId: getStreak
  summary: The user's current logging streak
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/StreakCalendar.yaml
