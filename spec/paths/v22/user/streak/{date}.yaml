post:
  tags:
  - Streaks
  operationId: updateStreak
  summary: Record streak activity for a date
  responses:
    '200':
      description: OK
      content: {}
  parameters:
  - name: date
    in: path
    required: true
    schema:
      type: string
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../../../components/schemas/StreakUpdate.yaml
