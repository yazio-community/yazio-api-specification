get:
  tags:
  - Rewards & Shop
  operationId: getWallet
  summary: The user's credit balance
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/Wallet.yaml
